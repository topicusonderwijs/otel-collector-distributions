name: Release

on:
  repository_dispatch:
    types: [publish-assets]

permissions:
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: show payload
        run: echo ${{ toJson(github.event.client_payload.releases) }}
      # - uses: sboschman/otel-collector-distributions/.github/actions/build-action@main
      #   name: Publish
      #   with:
      #     publish: 'true'
      #     token: ${{ secrets.GITHUB_TOKEN }}
